{
    "variables": {
        "var1": "{{env `LOGNAME`}}"
    },
    "builders": [
        {
            "type": "file",
            "content": "{{user `var1`}}",
            "target": "dummy"
        }
    ]
}